image: docker:28

services:
  - docker:28-dind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""

stages:
  - test

docker-test:
  stage: test

  script:
    - docker version
    - docker info