stages:
  - build
  - push

variables:
  IMAGE_NAME: alokjha1/document-approval-system

build-image:
  stage: build

  tags:
    - ec2

  script:
    - echo "Building Docker Image..."
    - docker build -t $IMAGE_NAME:latest .

push-image:
  stage: push

  tags:
    - ec2

  script:
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
    - docker push $IMAGE_NAME:latest